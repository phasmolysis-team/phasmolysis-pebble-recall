HOST=localhost
PORT=8080

PG__HOST=localhost
PG__PORT=5432
PG__USER=postgres
PG__DBNAME=pebble
PG__PASSWORD=
PG_URL="postgresql+asyncpg://${PG__USER}:${PG__PASSWORD}@${PG__HOST}:${PG__PORT}/${PG__DBNAME}"

PGDATA=./pgdata
INIT_DB_OPTIONS="-p ${PG__PORT}"
TEST_DBNAME=pebble_test

ORIGINS="http://localhost:5173,http://localhost:8080"
API_ROOT="/api"

AUTH__JWT_SECRET=""
AUTH__COOKIE_SECRET=""
# Generate Fernet keys with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

GEMINI_API_KEY=""
